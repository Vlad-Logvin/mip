import React from "react";

export default function NotEnoughPermission() {
    return (
        <div className="NotEnoughPermission text-center">
            <h1>You don't have enough permissions!</h1>
        </div>
    );
}
